# UI 开发规则

> Y3 UI 系统的特殊规则和常见陷阱

## 🔴 坐标系（最重要！）

**Y3 UI 原点在左下角！Y 轴向上！**

```
     Y↑
      │
      │
      └────→ X
    (0,0)
```

- ❌ **错误**：用左上角思维写坐标
- ✅ **正确**：记住 Y 值越大越靠上

## 🎯 官方组件（必须使用）

| type | 名称 | 用途 | 禁止替代方案 |
|------|------|------|-------------|
| 17 | SkillBtn | 技能按钮 | ❌ 手写技能释放/冷却逻辑 |
| 18 | BuffList | Buff列表容器 | ❌ 手写 Buff 显示 |
| 19 | BuffItem | Buff单项 | - |
| 20 | EquipSlot | 物品槽 | ❌ 手写物品栏 |

## ⚠️ 常见字段错误

| 控件 | ❌ 错误字段 | ✅ 正确字段 |
|------|------------|------------|
| 按钮文本 | `text` | `normal_text` |
| 物品槽背景 | `equip_bg_img: "none_ref"` | `equip_bg_img: "{bg子节点UID}"` |
| 进度条值 | 直接设置 value | 先 `set_max_...` 后 `set_current_...` |

## 📋 UI 生成流程

```
用户需求 → y3-ui-pipeline（统一入口）
    │
    ├─ 需要新建 UI → y3-ui-generator（生成 JSON）
    │       └─ 草图 → HTML 预览 → 转换 JSON
    │
    └─ 需要 Lua 代码 → y3-ui-official（API 参考）
```

### 自动触发规则

- 用户描述 `完整 UI 需求` 时，默认触发 `y3-ui-pipeline`
- 用户明确说 `只生成 UI JSON / 只创建画板 / 用 y3-ui-generator` 时，允许直达 `y3-ui-generator`
- 用户明确说 `只写 UI Lua / 只接点击悬停显示隐藏 / 用官方 UI API` 时，允许直达 `y3-ui-official`
- 如果同一句话里既包含 `新建 UI` 又包含 `交互逻辑`，仍然优先走 `y3-ui-pipeline`

### 统一口径

- `y3-ui-pipeline` = UI 统一入口
- `y3-ui-generator` = UI 资源生成
- `y3-ui-official` = UI 交互逻辑

### 禁止手写 UI JSON！

UI JSON 文件通常 300+ 行，必须使用 `y3-ui-generator` 技能通过 HTML 转换生成。

> 详细边界见 `knowledge/UI系统/UI技能自动路由规范.md`

## 📚 参考文档

| 文档 | 路径 |
|------|------|
| UI 字段规范 | `knowledge/05-UI字段规范.md` |
| 官方组件指南 | `knowledge/08-官方UI组件.md` |
| 控件适配规则 | `knowledge/16-UI控件适配规则.md` |
