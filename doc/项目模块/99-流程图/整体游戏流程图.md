# 整体游戏流程图

## 1. 说明

这张图只画当前仓库里“真实执行的游戏主流程”，不把保留入口画成主线节点。

图中重点标出：

- 地图启动
- 运行时初始化
- 主波次推进
- 挑战并行
- 成长与羁绊
- 资源变化
- 胜负结算
- UI 展示
- 地图数据输入

## 2. Mermaid 总流程图

```mermaid
flowchart TD
    subgraph 输入源
        A1[entry_config.lua 配置]
        A2[maps/EntryMap/unit ability projectile 等物编数据]
        A3[maps/EntryMap/ui 与 ui_res.lua]
    end

    subgraph 启动阶段
        B1[游戏启动]
        B2[main.lua]
        B3[require entry_runtime]
        B4[entry_runtime.bootstrap]
    end

    subgraph 初始化阶段
        C1[validate_config]
        C2[reset_state]
        C3[创建 hero 英雄对象]
        C4[创建 STATE 运行态容器]
        C5[创建 all_enemies 敌人集合]
        C6[创建 bond_runtime 羁绊运行时]
        C7[注册事件与调试命令]
        C8[启动主循环]
    end

    subgraph 主流程
        D1[start_wave 1]
        D2[主波次推进]
        D3[按区域批量生成主怪]
        D4[到时生成 Boss]
        D5[敌人 attack_move 到防线]
        D6[英雄与攻击技能输出伤害]
        D7[敌人死亡结算]
    end

    subgraph 并行系统
        E1[Q W E R 挑战入口]
        E2[挑战实例 active_challenges]
        E3[挑战敌人生成]
        E4[挑战成功或失败]
    end

    subgraph 成长系统
        F1[经验增长与等级推进]
        F2[skill_points 增长]
        F3[G 三选一]
        F4[攻击技能解锁或强化]
        F5[F 羁绊抽卡]
        F6[羁绊激活与动态效果]
    end

    subgraph 资源与输出
        G1[金币 木材 被动收入]
        G2[display_message 和 print]
        G3[GameHUD 与 GM 面板]
        G4[胜负结算]
    end

    subgraph 保留或并行资产
        H1[maps/EntryMap/global_trigger UI触发器资产]
        H2[global_script/global_main.lua 保留入口]
        H3[根目录 global_trigger 空索引]
    end

    A1 --> B4
    A2 --> C3
    A2 --> D3
    A2 --> D4
    A3 --> G3

    B1 --> B2
    B2 --> B3
    B3 --> B4

    B4 --> C1
    C1 --> C2
    C2 --> C3
    C2 --> C4
    C2 --> C5
    C2 --> C6
    C3 --> C7
    C4 --> C7
    C5 --> C8
    C6 --> C8
    C7 --> C8
    C8 --> D1

    D1 --> D2
    D2 --> D3
    D2 --> D4
    D3 --> D5
    D4 --> D5
    D5 --> D6
    D6 --> D7

    D7 --> F1
    D7 --> G1
    G1 --> F5

    F1 --> F2
    F2 --> F3
    F3 --> F4
    F4 --> D6
    F5 --> F6
    F6 --> D6
    F6 --> G1

    E1 --> E2
    E2 --> E3
    E3 --> D5
    E3 --> D7
    E3 --> E4
    E4 --> G1
    E4 --> F1

    D2 --> G2
    D7 --> G2
    F3 --> G2
    F5 --> G2
    C8 --> G3
    G1 --> G3

    D7 --> G4
    D2 --> G4

    H1 -. 并行存在 .-> G3
    H2 -. 当前未接入主线 .-> B2
    H3 -. 当前未接入主线 .-> B2
```

## 3. 图的阅读重点

这张图最重要的阅读结论有三个：

1. 主入口是 `main.lua -> entry_runtime.bootstrap()`，不是 `global_script/global_main.lua`。
2. 主战斗闭环是“波次/挑战 -> 击杀 -> 资源与经验 -> 升级/羁绊 -> 更强战斗力 -> 下一轮波次”。
3. UI、地图物编数据、地图内触发器资产都很重要，但它们不等于当前 Lua 主循环本身。
