# 整体游戏流程图

## 模块职责

用 agent 可执行视角展示启动链路、单局循环和模块关系。

## Source Of Truth

- 启动与循环：`maps/EntryMap/script/main.lua`、`maps/EntryMap/script/entry_runtime.lua`
- 配置与对象层：`maps/EntryMap/script/entry_config.lua`、`maps/EntryMap/script/entry_objects`
- 运行时子系统：`maps/EntryMap/script/entry_runtime_*.lua`、`maps/EntryMap/script/runtime_bonds.lua`

## 关键状态与数据流

- 启动流：`main.lua -> entry_runtime.bootstrap()`
- 主循环流：`STATE + timer update + reward_queue`
- 成长回流：击杀/挑战奖励 -> 经验/资源 -> `G/F/烙印/宝物` -> 战斗力回写
- 系统分层流：设计 -> 配置 -> 运行时 -> UI/资源

## 启动链路

```mermaid
flowchart TD
    A[游戏初始化] --> B[main.lua]
    B --> C[require entry_runtime]
    C --> D[加载 entry_config / entry_objects / runtime modules]
    D --> E[bootstrap]
    E --> F[validate_config]
    F --> G[reset_state]
    G --> H[create_hero + initialize_hero_progression]
    H --> I[register_runtime_events + register_dev_commands]
    I --> J[start_runtime_loops]
    J --> K[ensure_runtime_hud + ensure_gm_panel]
    K --> L[start_wave 1]
```

## 单局主循环

```mermaid
flowchart TD
    A[0.25秒循环] --> B[update_passive_resources]
    A --> C[update_wave]
    A --> D[update_challenges]
    A --> E[update_challenge_charges]
    A --> F[update_bond_effects]
    A --> G[update_attack_skills]
    A --> H[refresh_runtime_hud]
    A --> I[调试模式刷新GM]
```

## 单局推进

```mermaid
flowchart TD
    A[开始第N波] --> B[主怪批次刷新]
    B --> C[英雄自动战斗]
    C --> D[击杀/挑战成功 -> award_rewards]
    D --> E[经验/金币/木材增长]
    E --> F[升级后获得技能点]
    F --> G[G 三选一强化]
    E --> H[F 羁绊抽卡与效果回流]
    E --> I[奖励队列中的烙印/宝物轮次]
    B --> J[Boss到时登场]
    J --> K{是否击败Boss}
    K -- 否 --> C
    K -- 是且N小于5 --> L[立即切到下一波]
    K -- 是且N等于5 --> M[胜利结算]
```

## 模块关系

```mermaid
flowchart LR
    A[docs/design] --> B[entry_objects/*]
    B --> C[entry_config.lua]
    C --> D[entry_runtime.lua]
    D --> E[entry_runtime_battlefield.lua]
    D --> F[entry_runtime_progression.lua]
    D --> G[entry_runtime_attack_skills.lua]
    D --> H[entry_runtime_attack_upgrades.lua]
    D --> I[entry_runtime_hud.lua]
    B --> J[runtime_bonds.lua]
    D --> J
    K[ui_res.lua] --> I
    L[maps/EntryMap/ui] --> I
    M[物编资源目录] --> E
```

## 阅读重点

- 看主循环先读 `entry_runtime.lua`
- 看静态对象先读 `entry_objects/README.md`
- 看配置先读 `entry_config.lua`
- 看 `F` 系统先读 `runtime_bonds.lua`
- 看未来规划与缺口，回到 `maps/EntryMap/script/docs/项目模块/07-实现状态与路线图/实现状态与路线图.md`

## 当前已实现行为

- 启动链路、5 波主线、`G/F/挑战/宝物/烙印` 并行与互斥、运行时 HUD 和基础结算都已经能在图中对应到真实代码

## 当前实现边界

- 图中没有展开正式中央决策面板、成长武器、词缀节点和局外系统，因为这些仍未正式进入运行时

## Agent 修改建议与风险点

- 流程图只用于快速建立心智模型，具体字段和状态仍以代码与模块文档为准
